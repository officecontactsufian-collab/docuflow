
{
  "entities": {
    "Document": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Document",
      "type": "object",
      "description": "Represents an uploaded document in the PDF Spark application, which can be processed by various tools.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Document entity."
        },
        "originalFileName": {
          "type": "string",
          "description": "The original name of the file as uploaded by the user."
        },
        "storageReference": {
          "type": "string",
          "description": "A URI reference to the document's location in the storage system (e.g., Firebase Storage).",
          "format": "uri"
        },
        "mimeType": {
          "type": "string",
          "description": "The MIME type of the uploaded file (e.g., 'application/pdf', 'image/jpeg')."
        },
        "fileSizeKB": {
          "type": "number",
          "description": "The size of the document in kilobytes."
        },
        "uploadTimestamp": {
          "type": "string",
          "description": "The date and time when the document was uploaded.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current processing status of the document (e.g., 'uploaded', 'processing', 'processed', 'failed')."
        }
      },
      "required": [
        "id",
        "originalFileName",
        "storageReference",
        "mimeType",
        "fileSizeKB",
        "uploadTimestamp",
        "status"
      ]
    },
    "AIResult": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AIResult",
      "type": "object",
      "description": "Stores the output generated by AI content analysis tools, which can be shared publicly.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AIResult entity."
        },
        "creatorId": {
          "type": "string",
          "description": "The UID of the user who generated this result."
        },
        "toolName": {
          "type": "string",
          "description": "The name of the AI tool that generated this result."
        },
        "generatedContent": {
          "type": "string",
          "description": "The actual content generated by the AI tool."
        },
        "shareSlug": {
          "type": "string",
          "description": "A unique, SEO-friendly slug used to construct the public URL."
        },
        "createdAt": {
          "type": "string",
          "description": "The date and time when this AI result was created.",
          "format": "date-time"
        },
        "isPubliclyShareable": {
          "type": "boolean",
          "description": "A boolean flag indicating whether this AI result is enabled for public sharing."
        }
      },
      "required": [
        "id",
        "toolName",
        "generatedContent",
        "shareSlug",
        "createdAt",
        "isPubliclyShareable",
        "creatorId"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/documents/{documentId}",
        "definition": {
          "entityName": "Document",
          "schema": {
            "$ref": "#/backend/entities/Document"
          },
          "description": "Stores user-uploaded private PDF documents.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns the document."
            },
            {
              "name": "documentId",
              "description": "The unique identifier of the uploaded document."
            }
          ]
        }
      },
      {
        "path": "/public_ai_results/{shareSlug}",
        "definition": {
          "entityName": "AIResult",
          "schema": {
            "$ref": "#/backend/entities/AIResult"
          },
          "description": "Stores publicly shareable AI results, accessible by a unique shareSlug.",
          "params": [
            {
              "name": "shareSlug",
              "description": "A unique, SEO-friendly slug."
            }
          ]
        }
      }
    ]
  }
}
